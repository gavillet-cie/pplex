version: "3"

services:
  reverse-proxy:
    build: ./reverse-proxy
    image: reverse-proxy
    ports:
      - 80:8080
    restart: always

  nuxt:
    build: ./frontend
    depends_on:
      - reverse-proxy
    environment:
      BASE_URL: "http://pplex.gavillet-cie.com"
    image: nuxt
    restart: always

  cms:
    build: ./backend
    depends_on:
      - reverse-proxy
    image: cms
    restart: always

  mysql:
    environment:
      MYSQL_ROOT_PASSWORD: rootdbpassword
    depends_on:
      - reverse-proxy
    image: mariadb
    restart: always

  adminer:
    image: adminer
    restart: always
