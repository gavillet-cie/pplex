name: Release
on:
  push:
    branches: [ main ]

jobs:
  tagged-release:
    name: "Tagged Release"
    runs-on: "ubuntu-latest"

    steps:
      # ...
      - name: "Build & test"
        run: |
          echo "done!"

      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.ZIP_TOKEN }}"
          prerelease: false
          files: |
            update-client.sh
            update-backend.sh
